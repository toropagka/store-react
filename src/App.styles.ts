import styled from 'styled-components';

const StyledApp = styled.div`
  text-align: center;

  .App-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: calc(10px + 2vmin);
    margin: 28px 80px 50px;
  }

  .header-search__text {
  }
`;
export default StyledApp;
